---
title: eternt
description: We are here to stay, we make happy software. Awesome apps crafted with passion and precision.
template: splash
hero:
  tagline: We are here to stay, we make happy software.
  image:
    html: |
      <div class="eternt-hero">
        <style>
          .eternt-hero {
            position: relative;
            width: 320px;
            height: 380px;
            display: flex;
            justify-content: center;
            align-items: center;
          }

          /* Animated background orbs */
          .orb {
            position: absolute;
            border-radius: 50%;
            filter: blur(40px);
            opacity: 0.6;
            animation: orbFloat 8s ease-in-out infinite;
          }
          .orb-1 {
            width: 200px;
            height: 200px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            top: 10%;
            left: 0;
            animation-delay: 0s;
          }
          .orb-2 {
            width: 150px;
            height: 150px;
            background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            bottom: 10%;
            right: 0;
            animation-delay: -2s;
          }
          .orb-3 {
            width: 100px;
            height: 100px;
            background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            animation-delay: -4s;
          }

          /* Device showcase */
          .device-showcase {
            position: relative;
            z-index: 10;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: -20px;
          }

          /* Main iPhone */
          .iphone {
            position: relative;
            width: 140px;
            height: 280px;
            background: linear-gradient(145deg, #1c1c1e 0%, #2c2c2e 50%, #1c1c1e 100%);
            border-radius: 32px;
            padding: 8px;
            box-shadow:
              0 50px 100px -20px rgba(0, 0, 0, 0.5),
              0 0 40px rgba(99, 102, 241, 0.15),
              inset 0 0 0 1px rgba(255, 255, 255, 0.1);
            animation: mainFloat 4s ease-in-out infinite;
            z-index: 20;
          }
          .iphone::before {
            content: '';
            position: absolute;
            top: 12px;
            left: 50%;
            transform: translateX(-50%);
            width: 60px;
            height: 24px;
            background: #000;
            border-radius: 20px;
            z-index: 30;
          }
          .iphone-screen {
            width: 100%;
            height: 100%;
            background: linear-gradient(180deg, #1a1a2e 0%, #16213e 100%);
            border-radius: 24px;
            overflow: hidden;
            position: relative;
          }
          .screen-ui {
            position: absolute;
            inset: 0;
            display: flex;
            flex-direction: column;
            padding: 40px 12px 20px;
          }
          .ui-header {
            height: 20px;
            background: linear-gradient(90deg, rgba(99, 102, 241, 0.3) 0%, rgba(168, 85, 247, 0.3) 100%);
            border-radius: 10px;
            margin-bottom: 12px;
          }
          .ui-card {
            flex: 1;
            background: linear-gradient(135deg, rgba(99, 102, 241, 0.2) 0%, rgba(168, 85, 247, 0.2) 100%);
            border-radius: 16px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            margin-bottom: 10px;
            position: relative;
            overflow: hidden;
          }
          .ui-card::after {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.1), transparent);
            animation: shimmer 3s infinite;
          }
          .ui-buttons {
            display: flex;
            gap: 8px;
          }
          .ui-btn {
            flex: 1;
            height: 32px;
            border-radius: 16px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            animation: btnPulse 2s ease-in-out infinite;
          }
          .ui-btn:nth-child(2) {
            background: rgba(255, 255, 255, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
            animation-delay: 0.5s;
          }

          /* Side iPhones */
          .iphone-side {
            width: 100px;
            height: 200px;
            background: linear-gradient(145deg, #1c1c1e 0%, #2c2c2e 100%);
            border-radius: 24px;
            padding: 6px;
            box-shadow: 0 30px 60px -15px rgba(0, 0, 0, 0.4);
            opacity: 0.7;
            z-index: 10;
          }
          .iphone-left {
            transform: perspective(1000px) rotateY(25deg) translateX(20px);
            animation: sideFloatLeft 4s ease-in-out infinite;
          }
          .iphone-right {
            transform: perspective(1000px) rotateY(-25deg) translateX(-20px);
            animation: sideFloatRight 4s ease-in-out infinite;
          }
          .side-screen {
            width: 100%;
            height: 100%;
            background: linear-gradient(180deg, #0f0f23 0%, #1a1a2e 100%);
            border-radius: 18px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            gap: 8px;
            padding: 20px 8px;
          }
          .side-icon {
            width: 36px;
            height: 36px;
            background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            border-radius: 10px;
            animation: iconBounce 2s ease-in-out infinite;
          }
          .side-icon:nth-child(2) {
            background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
            animation-delay: 0.3s;
          }
          .side-icon:nth-child(3) {
            background: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%);
            animation-delay: 0.6s;
          }

          /* Floating elements */
          .floating-elements {
            position: absolute;
            inset: 0;
            pointer-events: none;
            z-index: 5;
          }
          .float-dot {
            position: absolute;
            width: 8px;
            height: 8px;
            background: #667eea;
            border-radius: 50%;
            animation: dotFloat 6s ease-in-out infinite;
          }
          .float-dot:nth-child(1) { top: 10%; left: 10%; animation-delay: 0s; }
          .float-dot:nth-child(2) { top: 20%; right: 15%; animation-delay: -1s; background: #f093fb; }
          .float-dot:nth-child(3) { bottom: 30%; left: 5%; animation-delay: -2s; background: #4facfe; }
          .float-dot:nth-child(4) { bottom: 15%; right: 10%; animation-delay: -3s; background: #43e97b; }
          .float-dot:nth-child(5) { top: 50%; left: 0; animation-delay: -4s; }

          /* Code brackets decoration */
          .code-bracket {
            position: absolute;
            font-size: 48px;
            font-weight: 200;
            color: rgba(99, 102, 241, 0.2);
            font-family: monospace;
            z-index: 1;
          }
          .bracket-left { left: -20px; top: 30%; }
          .bracket-right { right: -20px; bottom: 30%; }

          /* Animations */
          @keyframes orbFloat {
            0%, 100% { transform: translate(0, 0) scale(1); }
            33% { transform: translate(10px, -10px) scale(1.05); }
            66% { transform: translate(-5px, 5px) scale(0.95); }
          }
          @keyframes mainFloat {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-15px); }
          }
          @keyframes sideFloatLeft {
            0%, 100% { transform: perspective(1000px) rotateY(25deg) translateX(20px) translateY(0); }
            50% { transform: perspective(1000px) rotateY(25deg) translateX(20px) translateY(-10px); }
          }
          @keyframes sideFloatRight {
            0%, 100% { transform: perspective(1000px) rotateY(-25deg) translateX(-20px) translateY(0); }
            50% { transform: perspective(1000px) rotateY(-25deg) translateX(-20px) translateY(-10px); }
          }
          @keyframes shimmer {
            0% { left: -100%; }
            50%, 100% { left: 100%; }
          }
          @keyframes btnPulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.8; }
          }
          @keyframes iconBounce {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.1); }
          }
          @keyframes dotFloat {
            0%, 100% { transform: translateY(0) scale(1); opacity: 0.6; }
            50% { transform: translateY(-20px) scale(1.2); opacity: 1; }
          }
        </style>

        <div class="orb orb-1"></div>
        <div class="orb orb-2"></div>
        <div class="orb orb-3"></div>

        <span class="code-bracket bracket-left">{</span>
        <span class="code-bracket bracket-right">}</span>

        <div class="floating-elements">
          <div class="float-dot"></div>
          <div class="float-dot"></div>
          <div class="float-dot"></div>
          <div class="float-dot"></div>
          <div class="float-dot"></div>
        </div>

        <div class="device-showcase">
          <div class="iphone-side iphone-left">
            <div class="side-screen">
              <div class="side-icon"></div>
              <div class="side-icon"></div>
              <div class="side-icon"></div>
            </div>
          </div>

          <div class="iphone">
            <div class="iphone-screen">
              <div class="screen-ui">
                <div class="ui-header"></div>
                <div class="ui-card"></div>
                <div class="ui-buttons">
                  <div class="ui-btn"></div>
                  <div class="ui-btn"></div>
                </div>
              </div>
            </div>
          </div>

          <div class="iphone-side iphone-right">
            <div class="side-screen">
              <div class="side-icon"></div>
              <div class="side-icon"></div>
              <div class="side-icon"></div>
            </div>
          </div>
        </div>
      </div>
  actions:
    - text: Explore Our Apps
      link: /splashblock/wall-of-fame/
      icon: right-arrow
    - text: Get in Touch
      link: mailto:info@eternt.com
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

<div class="eternt-page">
<style>{`
  .eternt-page {
    --gradient-primary: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    --gradient-accent: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
    --gradient-success: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%);
    --glass-bg: rgba(255, 255, 255, 0.03);
    --glass-border: rgba(255, 255, 255, 0.08);
  }

  /* Section styling */
  .section-header {
    text-align: center;
    margin-bottom: 3rem;
  }
  .section-label {
    display: inline-block;
    padding: 0.5rem 1rem;
    background: var(--gradient-primary);
    border-radius: 20px;
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: white;
    margin-bottom: 1rem;
  }
  .section-title {
    font-size: 2.5rem;
    font-weight: 700;
    margin: 0 0 1rem 0;
    background: linear-gradient(135deg, #fff 0%, rgba(255,255,255,0.7) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
  .section-subtitle {
    font-size: 1.1rem;
    opacity: 0.7;
    max-width: 600px;
    margin: 0 auto;
    line-height: 1.6;
  }

  /* Stats section */
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    margin: 4rem 0;
    padding: 3rem;
    background: var(--glass-bg);
    border: 1px solid var(--glass-border);
    border-radius: 24px;
    backdrop-filter: blur(10px);
  }
  .stat-item {
    text-align: center;
  }
  .stat-number {
    font-size: 3rem;
    font-weight: 800;
    background: var(--gradient-primary);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    line-height: 1;
    margin-bottom: 0.5rem;
  }
  .stat-label {
    font-size: 0.9rem;
    opacity: 0.6;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  /* Services grid */
  .services-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
    margin: 2rem 0;
  }
  .service-card {
    padding: 2rem;
    background: var(--glass-bg);
    border: 1px solid var(--glass-border);
    border-radius: 20px;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }
  .service-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: var(--gradient-primary);
    transform: scaleX(0);
    transition: transform 0.3s ease;
  }
  .service-card:hover {
    transform: translateY(-5px);
    border-color: rgba(99, 102, 241, 0.3);
    box-shadow: 0 20px 40px -15px rgba(99, 102, 241, 0.2);
  }
  .service-card:hover::before {
    transform: scaleX(1);
  }
  .service-icon {
    width: 50px;
    height: 50px;
    background: var(--gradient-primary);
    border-radius: 14px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    margin-bottom: 1.25rem;
  }
  .service-card:nth-child(2) .service-icon { background: var(--gradient-accent); }
  .service-card:nth-child(3) .service-icon { background: var(--gradient-success); }
  .service-card:nth-child(4) .service-icon { background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%); }
  .service-title {
    font-size: 1.25rem;
    font-weight: 600;
    margin-bottom: 0.75rem;
  }
  .service-desc {
    opacity: 0.7;
    line-height: 1.6;
    font-size: 0.95rem;
  }

  /* Apps showcase */
  .apps-showcase {
    margin: 4rem 0;
  }
  .app-card {
    display: flex;
    align-items: center;
    gap: 3rem;
    padding: 3rem;
    background: linear-gradient(135deg, rgba(99, 102, 241, 0.1) 0%, rgba(168, 85, 247, 0.05) 100%);
    border: 1px solid rgba(99, 102, 241, 0.2);
    border-radius: 28px;
    margin-bottom: 2rem;
    transition: all 0.3s ease;
  }
  .app-card:hover {
    transform: translateY(-3px);
    box-shadow: 0 25px 50px -15px rgba(99, 102, 241, 0.2);
  }
  .app-icon-wrapper {
    flex-shrink: 0;
    width: 120px;
    height: 120px;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
    border-radius: 28px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 3.5rem;
    box-shadow: 0 15px 35px -10px rgba(99, 102, 241, 0.4);
    animation: iconFloat 3s ease-in-out infinite;
  }
  .app-content {
    flex: 1;
  }
  .app-badge {
    display: inline-block;
    padding: 0.35rem 0.75rem;
    background: rgba(99, 102, 241, 0.2);
    border-radius: 8px;
    font-size: 0.7rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: #a78bfa;
    margin-bottom: 0.75rem;
  }
  .app-name {
    font-size: 1.75rem;
    font-weight: 700;
    margin-bottom: 0.75rem;
  }
  .app-desc {
    opacity: 0.7;
    line-height: 1.6;
    margin-bottom: 1.5rem;
    font-size: 1rem;
  }
  .app-links {
    display: flex;
    gap: 1rem;
  }
  .app-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.5rem;
    background: var(--gradient-primary);
    border-radius: 12px;
    color: white;
    text-decoration: none;
    font-weight: 500;
    font-size: 0.9rem;
    transition: all 0.2s ease;
  }
  .app-link:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 20px -5px rgba(99, 102, 241, 0.4);
    color: white;
  }
  .app-link.secondary {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
  }
  .app-link.secondary:hover {
    background: rgba(255, 255, 255, 0.1);
    box-shadow: none;
  }

  /* CTA Section */
  .cta-section {
    text-align: center;
    padding: 4rem 2rem;
    background: linear-gradient(135deg, rgba(99, 102, 241, 0.15) 0%, rgba(168, 85, 247, 0.1) 100%);
    border-radius: 28px;
    border: 1px solid rgba(99, 102, 241, 0.2);
    margin: 4rem 0 2rem;
    position: relative;
    overflow: hidden;
  }
  .cta-section::before {
    content: '';
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(circle, rgba(99, 102, 241, 0.1) 0%, transparent 50%);
    animation: ctaPulse 8s ease-in-out infinite;
  }
  .cta-content {
    position: relative;
    z-index: 1;
  }
  .cta-title {
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 1rem;
  }
  .cta-text {
    opacity: 0.7;
    margin-bottom: 2rem;
    font-size: 1.1rem;
  }
  .cta-button {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1rem 2rem;
    background: var(--gradient-primary);
    border-radius: 14px;
    color: white;
    text-decoration: none;
    font-weight: 600;
    font-size: 1rem;
    transition: all 0.3s ease;
  }
  .cta-button:hover {
    transform: translateY(-3px);
    box-shadow: 0 15px 30px -10px rgba(99, 102, 241, 0.5);
    color: white;
  }

  @keyframes iconFloat {
    0%, 100% { transform: translateY(0) rotate(0deg); }
    50% { transform: translateY(-8px) rotate(2deg); }
  }
  @keyframes ctaPulse {
    0%, 100% { transform: translate(0, 0); }
    50% { transform: translate(2%, 2%); }
  }

  /* Responsive */
  @media (max-width: 768px) {
    .stats-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
      padding: 2rem;
    }
    .services-grid {
      grid-template-columns: 1fr;
    }
    .app-card {
      flex-direction: column;
      text-align: center;
      gap: 2rem;
    }
    .app-links {
      justify-content: center;
      flex-wrap: wrap;
    }
    .section-title {
      font-size: 2rem;
    }
  }
`}</style>

{/* Stats Section */}
<div class="stats-grid">
  <div class="stat-item">
    <div class="stat-number">100%</div>
    <div class="stat-label">Passion Driven</div>
  </div>
  <div class="stat-item">
    <div class="stat-number">24/7</div>
    <div class="stat-label">App Support</div>
  </div>
  <div class="stat-item">
    <div class="stat-number">5*</div>
    <div class="stat-label">Quality Focus</div>
  </div>
</div>

{/* Services Section */}
<div class="section-header">
  <span class="section-label">What We Do</span>
  <h2 class="section-title">Crafting Digital Experiences</h2>
  <p class="section-subtitle">We blend cutting-edge technology with thoughtful design to create apps that users genuinely love.</p>
</div>

<div class="services-grid">
  <div class="service-card">
    <div class="service-icon">📱</div>
    <div class="service-title">App Development</div>
    <div class="service-desc">Beautiful applications built with modern frameworks. No compromises, just awesome experiences.</div>
  </div>
  <div class="service-card">
    <div class="service-icon">✨</div>
    <div class="service-title">User-First Design</div>
    <div class="service-desc">Intuitive interfaces that feel natural. Every tap, swipe, and gesture carefully considered.</div>
  </div>
  <div class="service-card">
    <div class="service-icon">🔒</div>
    <div class="service-title">Privacy Focused</div>
    <div class="service-desc">Your data stays yours. We build apps with transparency and respect for user privacy.</div>
  </div>
  <div class="service-card">
    <div class="service-icon">🚀</div>
    <div class="service-title">Built to Last</div>
    <div class="service-desc">Modern architecture, clean code, and best practices. Apps that stand the test of time.</div>
  </div>
</div>

{/* Apps Section */}
<div class="apps-showcase">
  <div class="section-header">
    <span class="section-label">Our Creations</span>
    <h2 class="section-title">Apps We've Built</h2>
    <p class="section-subtitle">Each app is crafted with care, designed to bring joy and solve real problems.</p>
  </div>

  <div class="app-card">
    <div class="app-icon-wrapper">🧩</div>
    <div class="app-content">
      <span class="app-badge">Puzzle Game</span>
      <h3 class="app-name">Splash Block</h3>
      <p class="app-desc">A fun and engaging puzzle game for all ages. Challenge yourself with colorful blocks and addictive gameplay that keeps you coming back for more.</p>
      <div class="app-links">
        <a href="/splashblock/wall-of-fame/" class="app-link">Wall of Fame</a>
        <a href="/splashblock/privacy/" class="app-link secondary">Privacy Policy</a>
      </div>
    </div>
  </div>

  <div class="app-card">
    <div class="app-icon-wrapper" style="background: linear-gradient(135deg, #43e97b 0%, #38f9d7 50%, #4facfe 100%);">📝</div>
    <div class="app-content">
      <span class="app-badge">Word Game</span>
      <h3 class="app-name">TruWord</h3>
      <p class="app-desc">Challenge your vocabulary and have fun with words. A fresh take on word puzzles that keeps your mind sharp.</p>
      <div class="app-links">
        <a href="/truword/privacy/" class="app-link secondary">Privacy Policy</a>
      </div>
    </div>
  </div>
</div>

{/* CTA Section */}
<div class="cta-section">
  <div class="cta-content">
    <h2 class="cta-title">Ready to Build Something Amazing?</h2>
    <p class="cta-text">Let's create an app that users will love.</p>
    <a href="mailto:info@eternt.com" class="cta-button">
      Get in Touch
      <span>→</span>
    </a>
  </div>
</div>

</div>
